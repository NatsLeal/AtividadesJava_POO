public class MatematicaOperacoes {
    public double raizQuadrada(double n) {
        return Math.sqrt(n);
    }

    public double potencia(double base, int exp) {
        return Math.pow(base, exp);
    }
}
